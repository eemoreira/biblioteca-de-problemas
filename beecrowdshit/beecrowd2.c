#include <stdio.h>
int main(){
  int A, B, soma;
  printf("escreva seu A");
  scanf("%d", &A);
  printf("escreva seu B");
  scanf("%d", &B);
  soma = A + B;
  printf("SOMA = %d", soma);
return 0;
}
#include <bits/stdc++.h> 

using namespace std;


void solve(){
    int n,m; cin >> n >> m;
    int s[50][8];
    for(int i = 0; i < n; i++){
        for(int k = 0; k < m; k++){
            char temp; cin >> temp;
            s[i][k] = (int)temp;
            }
    }
    int difs[50][8];
    for(int i = 0; i < n; i++){
        for(int k = 0; k < m; k++){
            for(int j = 0; j < n; j++){
                difs[j][k] = l
            }
        }
        }
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t; cin >> t;
    while(t--){
    solve();
    }
    return 0;
}

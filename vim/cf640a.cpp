#include <bits/stdc++.h>

using namespace std;

const int MAX = 1e4;

void solve(){
    int n,count=0; cin >> n;
    int arr[MAX];
    int num[5];
    while(n != 1){
        num[k] = n
}
}

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int tc; cin >> tc;
    while(tc--) solve();
	return 0;
}

